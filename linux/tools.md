# Usefull linux tools

- `sudo apt install mc` - Midnight Commander, a visual file manager with dual-pane interface for easy file operations
- `sudo apt install htop` - Interactive process viewer and system monitor, enhanced version of top with better UI
